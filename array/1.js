const initialArray=[1,2,3];

initialArray.unshift(0);
initialArray.unshift(-1);

initialArray.push(15);

// console.log(initialArray);

//concatination

const finalArray = [4,3,5];

initialArray.concat(finalArray);

console.log(initialArray);
